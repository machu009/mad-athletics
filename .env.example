NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key — never expose client-side. Only use it in one-off
# seeding/admin scripts you run yourself, not in app runtime code.
SUPABASE_SERVICE_ROLE_KEY=
